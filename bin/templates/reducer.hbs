
const defaultState = {
	// TO DO: fill this in
}

export default (prevState = defaultState, action{{#if useTypescript}}: { type: string }{{/if}}) => {

  	let newState = { ...prevState }

  	switch (action.type) {

    default:
      	return prevState
  	}
}