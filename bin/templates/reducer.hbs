
const defaultState = {
  // TO DO: fill this in
}

export default (prevState = defaultState, action) => {

  let newState = { ...prevState }

  switch (action.type) {

    default:
      return prevState
  }

  return newState
}