import React from 'react'{{#if useSemicolons}};{{/if}}
import {{classCase component}} from './{{fileCase component}}'{{#if useSemicolons}};{{/if}}

export default {
  title: '{{titleCase component}}',
  component: {{classCase component}},
}{{#if useSemicolons}};{{/if}}

export const {{camelCase component}} = () => <{{classCase component}} />{{#if useSemicolons}};{{/if}}