import React, { useState, createContext, useMemo } from 'react'

type {{context}}Context = {

}

export const {{context}}Context = createContext<{{context}}Context>({

})

const {{context}}ContextProvider = (props: any) => {

    const {{lowerCaseInitial context}}ContextProvider = useMemo<{{context}}Context>(() => {
        return {

        }
    }, [

    ])

    return <{{context}}Context.Provider value={ {{lowerCaseInitial context}}ContextProvider }{...props} />
}

export default {{context}}ContextProvider