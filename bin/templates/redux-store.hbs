import { createStore, applyMiddleware, compose } from 'redux'
import reducer from './reducers'

// TO DO: Update initial state (second argument of createStore)
export default createStore(reducer, null)