import React, { useState, createContext, useMemo } from 'react'{{#if useSemicolons}};{{/if}}

type {{contextCase context}} = {

}{{#if useSemicolons}};{{/if}}

export const {{contextCase context}} = createContext<{{contextCase context}}>({

}){{#if useSemicolons}};{{/if}}

export const {{contextProviderCase context}} = (props: any) => {

    const {{contextProviderVarCase context}} = useMemo<{{contextCase context}}>(() => {
        return {

        }{{#if useSemicolons}};{{/if}}
    }, [

    ]){{#if useSemicolons}};{{/if}}

    return <{{contextCase context}}.Provider value={ {{contextProviderVarCase context}} }{...props} />{{#if useSemicolons}};{{/if}}
}

export default {{contextProviderCase context}}{{#if useSemicolons}};{{/if}}